import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  card: {
    maxWidth: 230,
    width: 230,
  },
  media: {
    height: 0,
    paddingTop: '100%',
    marginLeft: 10,
    marginRight: 10,
  },
}))

export default useStyles
