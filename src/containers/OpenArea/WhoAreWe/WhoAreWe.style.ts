import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  container: {
    margin: '20px 0px',
  },
  gridContainter: {
    padding: '0px 10px',
    width: '100%',
  },
  image: {
    height: 340,
    maxHeight: 340,
  },
}))

export default useStyles
